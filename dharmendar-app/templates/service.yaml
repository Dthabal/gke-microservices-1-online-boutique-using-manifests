apiVersion: v1
kind: Service
metadata:
  name: {{ include "dharmendar-app.fullname" . }}
  labels:
    {{- include "dharmendar-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dharmendar-app.selectorLabels" . | nindent 4 }}
