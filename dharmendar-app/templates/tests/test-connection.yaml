apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dharmendar-app.fullname" . }}-test-connection"
  labels:
    {{- include "dharmendar-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dharmendar-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
